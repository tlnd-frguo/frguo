package beans;

public class LogBean {

    public static void logBody(String message) {
        if (message == null) {
            message = "[Bean] No message";
        }
        System.out.println("[Bean]: " + message);
    }
}
